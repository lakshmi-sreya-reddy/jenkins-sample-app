version: "3.8"

services:
  web:
    image: lakshmisreyareddy/jenkins-sample-app:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    restart: always
    environment:
      - NODE_ENV=production
